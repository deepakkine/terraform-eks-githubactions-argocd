apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app
  namespace: ecommerce

spec:
  type: NodePort

  selector:
    app: ecommerce-app

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080